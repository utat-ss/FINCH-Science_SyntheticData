This readme file defines what each of the training runs are for.

- runs1: We are using this run to get a baseline of given a model x, we get results y.
- run2: Run1 but with a way bigger epsilon, normal distribution temperature sampling.